# CLAUDE.md

Claude agents should follow the canonical repository instructions in
`AGENTS.md`.

Required project rule: keep `STATUS.md` up to date whenever behavior, risks,
validation, or next steps change. Always update the `Last updated` field with
the current date and time (`YYYY-MM-DD HH:MM`) whenever `STATUS.md` is modified.
