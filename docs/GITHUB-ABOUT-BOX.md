# GitHub About Box — Suggested copy

Short description (one line):

LLM-r bridges Ableton Live and LLM agents through AbletonOSC + Modelito.

Long description (2–3 sentences):

LLM-r turns natural-language production requests into validated Ableton Live action plans. It provides a local FastAPI server, PyQt desktop GUI, macro system, live-state endpoints, and safe dry-run/approval controls for OSC execution through AbletonOSC.

Suggested topics (copy into GitHub topics):

- ableton-live
- abletonosc
- llm
- ai-music
- music-production
- automation
- fastapi
- python
- modelito
- agent-tools

Usage (quick): paste the short description into the repository About box. To set programmatically with the GitHub CLI:

```bash
gh repo edit --description "LLM-r bridges Ableton Live and LLM agents through AbletonOSC + Modelito."
gh repo edit --add-topic ableton-live --add-topic abletonosc --add-topic llm --add-topic ai-music --add-topic music-production --add-topic automation --add-topic fastapi --add-topic python --add-topic modelito --add-topic agent-tools
```
